# 公開前チェックリスト（testブランチ → 本番公開）

## 変更点の概要

### 主な変更内容
- **manifest.json**: `host_permissions`削除、`storage`権限追加
- **popup.js**: `htmlStructure`送信停止、認証トークン対応、注入不可ページの例外処理
- **API**: 認証、レート制限、求人ページ判定、ログマスキング追加
- **新規機能**: レート制限、求人ページ判定、ログマスキング

### 変更ファイル一覧
- 修正: 7ファイル
- 新規: 7ファイル（ライブラリ3、ドキュメント4）

## ⚠️ 公開前に確認すべき重要な点

### 1. 認証の扱い（重要度: 高）

**現状の実装:**
- 認証トークンがなくても`anonymous`ユーザーとして動作可能
- レート制限はIPベースまたはユーザーIDベース

**確認事項:**
- [ ] 認証なしでも拡張機能が動作するか（動作する ✅）
- [ ] 匿名ユーザーでも保存が可能か（可能 ✅）
- [ ] レート制限が適切か（30件/分、Gemini API用10件/分）
  - **注意**: 一気に15件保存しても問題ないように緩和済み ✅

**リスク:**
- 認証が必須になっている場合、既存ユーザーが使えなくなる可能性
- **現状**: 匿名ユーザーも許可されているため問題なし ✅

### 2. htmlStructure送信停止の影響（重要度: 高）

**変更内容:**
- `htmlStructure`の送信を完全に停止
- Gemini APIのプロンプトからHTML構造情報を除外

**確認事項:**
- [ ] Gemini APIの抽出精度が下がっていないか（要テスト）
- [ ] 既存の保存済みデータに影響はないか（影響なし ✅）

**リスク:**
- 抽出精度の低下により、ユーザー体験が悪化する可能性
- **推奨**: 公開前に複数の求人サイトでテスト実施

### 3. 求人ページ判定の影響（重要度: 中）

**変更内容:**
- サーバー側で求人ページ判定を実装
- `non_job`の場合はエラーを返す（強制保存オプションあり）

**確認事項:**
- [ ] 誤判定が多すぎないか（要テスト）
- [ ] 一覧ページが適切に処理されるか（`job_list`として保存 ✅）
- [ ] 強制保存オプションが動作するか（実装済み ✅）

**リスク:**
- 誤判定により、正当な求人ページが保存できなくなる可能性
- **推奨**: 公開前に様々な求人サイトでテスト実施

### 4. レート制限の影響（重要度: 中）

**変更内容:**
- Firestoreカウンタ方式のレート制限を実装
- 10件/分（通常）、5件/分（Gemini API）

**確認事項:**
- [ ] レート制限が正常に動作するか（要テスト）
- [ ] 制限が厳しすぎないか（30件/分に緩和済み ✅）
- [ ] 一気に15件保存できるか（可能 ✅）
- [ ] Firestoreの読み書き負荷が高くないか（要監視）

**リスク:**
- ~~レート制限が厳しすぎると、ユーザーが使いにくくなる~~ → 30件/分に緩和済み ✅
- Firestoreの読み書きが増えるため、コストが上がる可能性

### 5. 後方互換性（重要度: 高）

**確認事項:**
- [ ] 既存のFirestoreデータ構造と互換性があるか（互換性あり ✅）
- [ ] 新規フィールド（`userId`, `pageType`, `pageTypeScore`）が既存データに影響しないか（影響なし ✅）
- [ ] 既存のWebアプリが正常に動作するか（動作する ✅）

**リスク:**
- 既存データとの不整合により、エラーが発生する可能性
- **現状**: 新規フィールドはオプショナルなため問題なし ✅

### 6. 環境変数の設定（重要度: 高）

**必要な環境変数:**
- `EXTENSION_ID`: CORS制限用（オプション、開発環境では不要）
- `GEMINI_API_KEY`: Gemini API用（必須）
- Firebase環境変数: 既存のまま（必須）

**確認事項:**
- [ ] Netlify環境変数に`EXTENSION_ID`が設定されているか（公開後に設定）
- [ ] 既存の環境変数が正しく設定されているか（確認済み ✅）

**リスク:**
- `EXTENSION_ID`が未設定の場合、開発環境では`*`が許可される（問題なし ✅）
- 本番環境では拡張機能IDを設定することを推奨

### 7. Firestore Security Rules（重要度: 高）

**現状:**
- Security Rulesの実装状況を確認する必要がある

**確認事項:**
- [ ] Firestore Security Rulesが設定されているか（要確認）
- [ ] ユーザーごとの読み書き制限が適切か（要確認）
- [ ] `rateLimits`コレクションへの書き込みが許可されているか（要確認）

**リスク:**
- Security Rulesが不適切だと、データが不正にアクセスされる可能性
- **推奨**: 公開前にSecurity Rulesを設定・確認

## 公開前のテスト項目

### 機能テスト
- [ ] 拡張機能が正常に動作するか（各種求人サイトでテスト）
- [ ] 認証なしで保存が可能か
- [ ] レート制限が正常に動作するか
- [ ] 求人ページ判定が正常に動作するか
- [ ] 注入不可ページで適切なエラーが表示されるか
- [ ] Webアプリで保存した求人が正常に表示されるか

### パフォーマンステスト
- [ ] APIの応答時間が適切か（レート制限の影響を確認）
- [ ] Firestoreの読み書き負荷が適切か
- [ ] Gemini APIの呼び出しが正常に動作するか

### セキュリティテスト
- [ ] 認証なしでの不正アクセスが防げているか
- [ ] レート制限が正常に機能しているか
- [ ] CORS制限が適切に設定されているか

## 公開手順

### 1. 事前準備
1. [ ] すべてのテスト項目を実施
2. [ ] Firestore Security Rulesを設定・確認
3. [ ] Netlify環境変数に`EXTENSION_ID`を設定（公開後）
4. [ ] アイコン画像とスクリーンショットを準備

### 2. 公開
1. [ ] `chrome-extension`フォルダをZIP化（`.git`、`config.js`を除外）
2. [ ] Chrome Web Store Developer Dashboardでアップロード
3. [ ] 説明文、権限説明文、Data Safetyを入力
4. [ ] 審査を送信

### 3. 公開後
1. [ ] 審査結果を確認
2. [ ] 公開後、動作確認を実施
3. [ ] エラーログを監視
4. [ ] ユーザーフィードバックを収集

## 推奨される段階的公開

### Phase 1: 限定公開（推奨）
- 最初は限定公開でテスト
- 問題がなければ一般公開

### Phase 2: モニタリング
- 公開後1週間はエラーログを重点的に監視
- ユーザーフィードバックを収集

### Phase 3: 改善
- 問題があれば迅速に修正
- バージョンアップで対応

## 結論

### ✅ 公開可能な点
- 認証はオプション（匿名ユーザーも許可）
- 既存データとの互換性あり
- 後方互換性あり
- セキュリティ対策を実装済み

### ⚠️ 公開前に確認すべき点
- Gemini APIの抽出精度（htmlStructure削除の影響）
- 求人ページ判定の精度（誤判定の有無）
- Firestore Security Rulesの設定
- レート制限の動作確認

### 🔴 公開前に必須の作業
1. **Firestore Security Rulesの設定**（最重要）
2. **複数の求人サイトでの動作テスト**
3. **レート制限の動作確認**
4. **Netlify環境変数の確認**

**総合判断**: 基本的には公開可能ですが、上記の確認事項を実施してから公開することを強く推奨します。
