# LLM機能デバッグガイド

## 問題の可能性

### 1. APIキーが設定されていない
- **確認方法**: サーバーログで `[LLM] API_KEY存在確認: false` が表示される
- **対処**: `.env.local` または Netlify環境変数に `GEMINI_API_KEY` を設定

### 2. LLM処理が実行されていない
- **確認方法**: サーバーログで `[LLM] refineWithGemini 開始` が表示されない
- **対処**: API Routeのログを確認

### 3. LLM処理は実行されているが、結果が変わらない
- **確認方法**: サーバーログで `[LLM] ルールベース結果と比較: 変更なし` が表示される
- **原因の可能性**:
  - LLMがルールベースと同じ結果を返している
  - `??`演算子の問題（修正済み）

### 4. パースエラー
- **確認方法**: サーバーログで `❌ LLMレスポンスのパースエラー` が表示される
- **対処**: レスポンステキストを確認して、JSON形式を修正

## デバッグログの確認方法

### ローカル開発環境
```bash
npm run dev
```

サーバーログで以下を確認：
```
[LLM] refineWithGemini 開始
[LLM] API_KEY存在確認: true/false
[LLM] genAI存在確認: true/false
[LLM] ルールベース結果: {...}
[LLM] プロンプト長: XXX 文字
[LLM] Gemini処理時間: XXXms
[LLM] レスポンステキスト（最初の500文字）: ...
[LLM] LLM整形後結果: {...}
[LLM] ルールベース結果と比較: 変更あり/変更なし
```

### Netlify環境
- Netlifyダッシュボード → Functions → Logs で確認

## 修正内容

### parseLLMResponse関数の修正
- **問題**: `??`演算子により、LLMが`null`を返した場合にfallbackが使われていた
- **修正**: `!== undefined` を使用して、`null`は有効な値として扱うように変更

## 次のステップ

1. サーバーログを確認して、どの段階で問題が発生しているか特定
2. LLM処理が実行されているか確認
3. LLMのレスポンスを確認
4. 結果が変わっているか確認
